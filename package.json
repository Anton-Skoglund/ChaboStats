{
  "name": "chalmershouses",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd frontend && npm install && npm run build",
    "deploy": "npm run build && npx gh-pages -d frontend/dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0"
  },
  "type": "module",
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}